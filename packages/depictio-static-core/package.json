{
  "name": "depictio-static-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Serverless bundle runtime core: manifest contract, URI resolution, query engine + kernels. Pure TypeScript — no React, no DOM requirement — so it is unit-testable headlessly and consumable by the viewer's static runtime.",
  "sideEffects": false,
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hyparquet": "^1.27.1",
    "hyparquet-compressors": "^1.1.1"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^3.0.0"
  }
}
