[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "plotly-upset"
version = "0.1.0"
description = "UpSet plot visualizations with Plotly — interactive set intersection plots with annotation tracks"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Depictio Contributors" },
]
keywords = [
    "upset",
    "set-intersection",
    "plotly",
    "bioinformatics",
    "visualization",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Topic :: Scientific/Engineering :: Visualization",
]
dependencies = [
    "plotly>=5.5.0",
    "numpy>=1.22.0",
    "pandas>=1.4.0",
]

[project.optional-dependencies]
polars = ["polars>=0.20.0"]
notebook = [
    "jupyterlab>=4.0",
    "ipywidgets>=8.0",
]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "polars>=0.20.0",
    "jupyterlab>=4.0",
    "ipywidgets>=8.0",
]

[project.urls]
Homepage = "https://github.com/depictio/plotly-upset"
Repository = "https://github.com/depictio/plotly-upset"
Issues = "https://github.com/depictio/plotly-upset/issues"

[tool.hatch.build.targets.wheel]
packages = ["plotly_upset"]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.pytest.ini_options]
testpaths = ["tests"]
