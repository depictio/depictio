/** Brand color palette. Defined in depictio-react-core so the shared components
 *  (component-type accents, chrome) use the same values; re-exported here
 *  because this app has always imported it from `profile/colors`. */
export { brandColors } from 'depictio-react-core';
