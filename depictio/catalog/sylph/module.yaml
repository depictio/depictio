# sylph. Lightweight: identity is derived from the nf-core meta.yml that
# `nf_core_url` points at (not duplicated). Override a field here only on drift.
id: sylph
name: sylph
nf_core_url: https://github.com/nf-core/modules/tree/master/modules/nf-core/sylph/profile
