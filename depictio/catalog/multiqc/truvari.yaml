id: multiqc_truvari
name: Structural-variant benchmark (Truvari)
origin_tool: Truvari
mode: truvari
description: Structural-variant accuracy per callset (precision vs recall, TP / FP / FN classifications) as Truvari reports it through MultiQC.
find: { path_glob: "**/multiqc/multiqc_data/multiqc.parquet" }
# reuse the bundled catalog-conformance MultiQC parquet (it carries the happy/
# sompy/truvari benchmark sections generated by scripts/multiqc_stubs.py);
# `section` selects this module's plot anchors out of the shared report.
fixture: ../../projects/init/catalog_conformance/run_1/multiqc/multiqc_data/multiqc.parquet
renders_as:
  - { component: multiqc, section: truvari }
