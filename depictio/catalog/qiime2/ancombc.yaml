id: qiime2_ancombc
mode: composition/ancombc
description: >-
  ANCOM-BC differential-abundance results. The tool writes one CSV per statistic
  per contrast; the recipe joins + melts them into a long table.
nf_core_url: https://github.com/nf-core/modules/tree/master/modules/nf-core/qiime2/ancombc
find: { path_glob: "**/qiime2/ancombc/differentials/*/lfc_slice.csv" }
recipe: nf-core/ampliseq/ancombc.py
fixture: nf-core/ampliseq/2.14.0/ancombc_habitat_level2.tsv
renders_as:
  - { component: advanced_viz, kind: volcano, roles: {feature_id: id, effect_size: lfc, significance: q_val} }
  - { component: advanced_viz, kind: da_barplot, roles: {feature_id: id, contrast: contrast, lfc: lfc} }
  - { component: advanced_viz, kind: qq, roles: {p_value: p_val} }
