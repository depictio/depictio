# iVar. Lightweight on purpose: identity (homepage, bio.tools id, EDAM topics)
# already lives in the nf-core meta.yml that `nf_core_url` points at — we don't
# duplicate it. Declare an identity field here only to *override* a stale
# meta.yml. The depictio glue (find/recipe/renders_as) lives in the output YAMLs.
id: ivar
name: iVar
nf_core_url: https://github.com/nf-core/modules/tree/master/modules/nf-core/ivar/variants
biotools_url: https://bio.tools/andersen-lab_ivar # from nf-core meta.yml identifier
