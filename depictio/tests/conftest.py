from depictio.tests.fixtures.v1.endpoints.dashboards import dashboards
"""
import pytest
from fastapi.testclient import TestClient
from depictio.api.main import app

@pytest.fixture(scope=module)
def client():
    client = TestClient(app)
    yield client
"""