# nf-core/ampliseq Template - Microbial Community Analysis
# ============================================================================
# This is a TEMPLATE project configuration. Use with:
#   depictio-cli run --template nf-core/ampliseq/2.14.0 --data-root /path/to/your/data
#
# The original project.yaml (with hardcoded paths) is kept as a fixture for
# Docker init. This template.yaml uses {DATA_ROOT} placeholders for reuse.
# ============================================================================

# Template metadata (parsed and removed before Project model validation)
template:
  template_id: "nf-core/ampliseq/2.14.0"
  description: "nf-core/ampliseq microbial community analysis template for 16S/ITS amplicon sequencing"
  version: "1.0.0"
  variables:
    - name: "DATA_ROOT"
      description: "Root directory containing ampliseq pipeline output data"
      required: true
  expected_files:
    # User-provided file (not recipe-generated)
    - relative_path: "merged_metadata.tsv"
      description: "Sample metadata with sample IDs and experimental factors"
      format: "TSV"
      columns: ["sample", "name", "habitat"]
    # Raw QIIME2 outputs (recipe inputs — transformed automatically during process)
    - relative_path: "qiime2/alpha-rarefaction/faith_pd.csv"
      description: "Raw QIIME2 rarefaction CSV (input for alpha_rarefaction recipe)"
      format: "CSV"
      columns: ["sample-id"]
    - relative_path: "qiime2/barplot/level-2.csv"
      description: "Raw QIIME2 barplot CSV (input for taxonomy_composition recipe)"
      format: "CSV"
      columns: ["index"]
    - relative_path: "qiime2/ancom/Category-habitat-ASV/data.tsv"
      description: "Raw ANCOM results (input for ancom_volcano recipe)"
      format: "TSV"
      columns: ["id", "W", "clr"]
    - relative_path: "qiime2/rel_abundance_tables/rel-table-ASV_with-DADA2-tax.tsv"
      description: "Taxonomy table with DADA2 annotations (input for ancom_volcano recipe)"
      format: "TSV"
      columns: ["ID"]
    - relative_path: "diversity/alpha_diversity/faith_pd_vector/metadata.tsv"
      description: "Raw QIIME2 Faith PD vector (input for alpha_diversity recipe)"
      format: "TSV"
      columns: ["id", "faith_pd"]
    - relative_path: "rel_abundance_tables/rel-table-2.tsv"
      description: "Raw relative abundance table (input for taxonomy_rel_abundance recipe)"
      format: "TSV"
      columns: ["#OTU ID"]
    - relative_path: "ancombc/differentials/Category-habitat-level-2/lfc_slice.csv"
      description: "ANCOM-BC log-fold change slice (input for ancombc recipe)"
      format: "CSV"
      columns: ["id"]
  expected_directories:
    - relative_path: "run_*"
      description: "Sequencing run directories containing pipeline outputs"
      glob_pattern: true
    - relative_path: "run_*/multiqc_data"
      description: "MultiQC output directories within each run"
      glob_pattern: true

# ============================================================================
# PROJECT CONFIGURATION
# ============================================================================

name: "Ampliseq Microbial Community Analysis"
project_type: "advanced"
is_public: true
workflows:
  - name: "ampliseq"
    version: "2.14.0"
    engine:
      name: "nextflow"
      version: "24.10.4"
    catalog:
      name: "nf-core"
      url: "https://nf-co.re"
    repository_url: "https://github.com/nf-core/ampliseq"
    data_location:
      structure: "sequencing-runs"
      runs_regex: "run_*"
      locations:
        - "{DATA_ROOT}"
    data_collections:
      - data_collection_tag: "multiqc_data"
        description: "MultiQC report data"
        config:
          type: "MultiQC"
          scan:
            mode: "recursive"
            scan_parameters:
              regex_config:
                pattern: "multiqc_data/multiqc.parquet"
          dc_specific_properties:
            s3_location: null
            modules:
              - "cutadapt"
              - "fastqc"
            plots:
              cutadapt:
                - "Filtered Reads"
                - "Trimmed Sequence Lengths (5')":
                    - "Counts"
                    - "Obs/Exp"
              fastqc:
                - "Sequence Counts"
                - "Sequence Quality Histograms"
                - "Per Sequence Quality Scores"
                - "Per Sequence GC Content":
                    - "Percentages"
                    - "Counts"
                - "Per Base N Content"
                - "Sequence Length Distribution"
                - "Sequence Duplication Levels"
                - "Overrepresented sequences by sample"
                - "Top overrepresented sequences"
                - "Adapter Content"
                - "Status Checks"

      - data_collection_tag: "metadata"
        description: "Sample metadata for ampliseq analysis"
        config:
          type: "Table"
          metatype: "Metadata"
          scan:
            mode: "single"
            scan_parameters:
              filename: "{DATA_ROOT}/merged_metadata.tsv"
          dc_specific_properties:
            format: "TSV"
            polars_kwargs:
              separator: "\t"
              try_parse_dates: true
            columns_description:
              "sample": "Sample identifier (SRR accession)"
              "name": "Sample name"
              "habitat": "Sample habitat type"
              "Riv_vs_Gro": "Habitat comparison group 1"
              "Sed_vs_Soil": "Habitat comparison group 2"
              "sampling_date": "Sample collection date (YYYY-MM-DD)"

      # ----------------------------------------------------------------
      # Recipe-transformed data collections
      # These read raw QIIME2 outputs and transform them during process
      # ----------------------------------------------------------------

      - data_collection_tag: "alpha_rarefaction"
        description: "Alpha diversity rarefaction curves (from raw QIIME2 CSV)"
        config:
          type: "Table"
          source: "transformed"
          transform:
            recipe: "nf-core/ampliseq/alpha_rarefaction.py"
          dc_specific_properties:
            format: "TSV"
            columns_description:
              "sample": "Sample identifier"
              "depth": "Sequencing depth"
              "iter": "Iteration number"
              "faith_pd": "Faith's Phylogenetic Diversity metric"

      - data_collection_tag: "taxonomy_composition"
        description: "Taxonomic composition across samples (from raw QIIME2 barplot)"
        config:
          type: "Table"
          source: "transformed"
          transform:
            recipe: "nf-core/ampliseq/taxonomy_composition.py"
          dc_specific_properties:
            format: "TSV"
            columns_description:
              "sample": "Sample identifier"
              "taxonomy": "Taxonomic classification (Kingdom;Phylum)"
              "count": "Raw sequence count for this taxonomy"
              "habitat": "Habitat type of the sample"

      - data_collection_tag: "ancom_volcano"
        description: "ANCOM differential abundance (from raw ANCOM results + taxonomy)"
        config:
          type: "Table"
          source: "transformed"
          transform:
            recipe: "nf-core/ampliseq/ancom_volcano.py"
          dc_specific_properties:
            format: "TSV"
            columns_description:
              "id": "ASV identifier"
              "taxonomy": "Taxonomic classification (Kingdom;Phylum)"
              "Kingdom": "Kingdom level classification"
              "Phylum": "Phylum level classification"
              "W": "W statistic from ANCOM analysis"
              "clr": "Centered log-ratio transformed abundance"

      - data_collection_tag: "alpha_diversity"
        description: "Per-sample alpha diversity (from raw QIIME2 Faith PD vector)"
        config:
          type: "Table"
          source: "transformed"
          transform:
            recipe: "nf-core/ampliseq/alpha_diversity.py"
          dc_specific_properties:
            format: "TSV"
            columns_description:
              "sample": "Sample identifier"
              "habitat": "Habitat type"
              "faith_pd": "Faith's Phylogenetic Diversity (single value per sample)"

      - data_collection_tag: "taxonomy_rel_abundance"
        description: "Taxonomic relative abundance at Phylum level (from raw abundance table)"
        config:
          type: "Table"
          source: "transformed"
          transform:
            recipe: "nf-core/ampliseq/taxonomy_rel_abundance.py"
          dc_specific_properties:
            format: "TSV"
            columns_description:
              "sample": "Sample identifier"
              "taxonomy": "Taxonomic classification (Kingdom;Phylum)"
              "rel_abundance": "Relative abundance (0-1)"
              "habitat": "Habitat type"
              "Kingdom": "Kingdom level"
              "Phylum": "Phylum level"

      - data_collection_tag: "ancombc_results"
        description: "ANCOM-BC differential abundance at Phylum level (from 5 raw CSV slices)"
        config:
          type: "Table"
          source: "transformed"
          transform:
            recipe: "nf-core/ampliseq/ancombc.py"
          dc_specific_properties:
            format: "TSV"
            columns_description:
              "id": "Taxonomy identifier"
              "contrast": "Statistical contrast (habitat comparison)"
              "lfc": "Log-fold change"
              "q_val": "FDR-adjusted p-value"
              "w": "ANCOM-BC test statistic"
              "Kingdom": "Kingdom level"
              "Phylum": "Phylum level"
              "neg_log10_qval": "-log10(q-value) for volcano plot"
              "significant": "q < 0.05"

# ============================================================================
# RUNTIME LINKS CONFIGURATION
# ============================================================================
# Links use data_collection_tag references (resolved to IDs at instantiation)
# ============================================================================

links:
  # Link 1: Metadata -> MultiQC (for MultiQC component filtering)
  - source_dc_tag: "metadata"
    source_column: "sample"
    target_dc_tag: "multiqc_data"
    target_type: "multiqc"
    link_config:
      resolver: "sample_mapping"
      target_field: "sample_name"
    description: "Filter MultiQC by metadata samples (habitat selections)"
    enabled: true

  # Link 2: Metadata -> Alpha Rarefaction (for Faith PD figure)
  - source_dc_tag: "metadata"
    source_column: "sample"
    target_dc_tag: "alpha_rarefaction"
    target_type: "table"
    link_config:
      resolver: "direct"
      target_field: "sample"
    description: "Filter alpha rarefaction by metadata samples"
    enabled: true

  # Link 3: Metadata -> Taxonomy Composition (for taxonomy figure)
  - source_dc_tag: "metadata"
    source_column: "sample"
    target_dc_tag: "taxonomy_composition"
    target_type: "table"
    link_config:
      resolver: "direct"
      target_field: "sample"
    description: "Filter taxonomy by metadata samples"
    enabled: true

  # Link 4: Metadata -> Alpha Diversity
  - source_dc_tag: "metadata"
    source_column: "sample"
    target_dc_tag: "alpha_diversity"
    target_type: "table"
    link_config:
      resolver: "direct"
      target_field: "sample"
    description: "Filter alpha diversity by metadata samples"
    enabled: true

  # Link 5: Metadata -> Taxonomy Rel Abundance
  - source_dc_tag: "metadata"
    source_column: "sample"
    target_dc_tag: "taxonomy_rel_abundance"
    target_type: "table"
    link_config:
      resolver: "direct"
      target_field: "sample"
    description: "Filter taxonomy rel abundance by metadata samples"
    enabled: true

  # Note: ANCOM volcano is not linked because it shows
  # habitat-independent differential abundance analysis and has no sample column
