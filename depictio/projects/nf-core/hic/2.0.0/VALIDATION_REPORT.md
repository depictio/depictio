# nf-core/hic 2.0.0: template validation report

**Date:** 2026-09-17
**Worktree / branch:** `depictio-worktrees/feat-nfcore-templates-lot2`
**Validator:** local recipe/model validation against the real AWS megatest data
(`~/Data/depictio-nfcore/hic/2.0.0/megatest/`); `depictio-cli run --dry-run`
against a running local instance. No server-side ingestion was performed for
this report (out of scope for this pass, see Ingestion status below).

## Goal

Build the hic 2.0.0 template plus four new catalog tools (`cooler`,
`cooltools`, `hicexplorer`, and the `multiqc/hicpro` panel). hic is the pilot
for the `contact_map` advanced-viz kind: a binned, ICE-balanced contact-matrix
recipe is bound to it here while the kind itself was being built in parallel
by another agent in the same shared worktree.

## Data used

AWS megatest run `results-b4d89cfacf97a5835fba804887cf0fc7e0449e8d` (the 2.0.0
release tag). One mouse ES-cell sample, `HIC_ES_4`, three FASTQ pairs HiC-Pro
merges before mapping.

```bash
bash depictio/projects/nf-core/hic/2.0.0/download_test_data.sh
```

The manifest (`megatest.yaml`) fetches the samplesheet, software versions, the
raw MultiQC inputs (fastqc, hicpro stats), and the tables behind the four
dedicated tabs: `contact_maps/` (cooler dump triplets + bins), `compartments/`
(cooltools eigenvector/eigenvalues), `tads/` (cooltools insulation, two
resolutions x three window sizes each) and `distance_decay/` (hicexplorer's
`hicPlotDistVsCounts` table).

## MultiQC was reprocessed: 1.13 to 1.35

hic 2.0.0 published MultiQC 1.13, which writes `mqc_*.txt` and no parquet.
Depictio reads only `multiqc.parquet`, so the MultiQC tab is bound to a report
regenerated by this repository's `multiqc_reprocess` script. `REPROCESSED.json`
records two contributing modules: `fastqc` and `hicpro`. The reprocessed
parquet fixture (50 KB) is committed at
`multiqc/multiqc_data/multiqc.parquet` for local testing without a full
megatest fetch.

`plots:`/`selected_plot:` values in `template.yaml` and `dashboards/base.yaml`
use MultiQC's bare `section.name` (e.g. `Read Mapping`), not the plot's
internal `title` config string (e.g. `HiC-Pro: Mapping Statistics`, a
display-only field MultiQC never persists as a matching key), confirmed by
grepping the installed `multiqc` package source for `add_section(name=...)`.

## Recipe validation against real data

All five recipes were run against the real megatest files (raw scan simulated
with `include_file_paths`, `pl.concat(..., how="diagonal_relaxed")` to mirror
`align_lazy_schemas()`'s per-file null-fill union), and every output frame's
schema matches its `EXPECTED_SCHEMA` exactly:

| Recipe | Rows | Cols | Notes |
|---|---|---|---|
| `cooler/contact_matrix.py` | 696,939 | 9 | finest resolution only (500 kb), intra-chromosomal pairs only, joined from an 11M-row sparse triplet |
| `cooltools/eigenvector.py` | 16,375 | 9 | both resolutions the run scanned (250 kb / 500 kb) |
| `cooltools/eigenvalues.py` | 44 | 6 | per-chromosome, both resolutions |
| `cooltools/insulation.py` | 613,314 | 12 | both resolutions (20 kb / 40 kb), windows discovered dynamically per file (300 kb/500 kb/1 Mb vs 600 kb/1 Mb/2 Mb, non-overlapping except 1 Mb) |
| `hicexplorer/distance_decay.py` | 13 | 7 | one resolution (250 kb), the one `hicPlotDistVsCounts` ran at |

`uv run ruff format` and `uv run ruff check` pass on all 6 new/edited `.py`
files (5 catalog recipes + the pipeline-local `hic/recipes/samples.py`).
`uv run pre-commit run --files <those + all new yaml/md/sh>` passes (trim
trailing whitespace, fix end of files, check yaml, check-added-large-files,
ruff, ruff-format, shellcheck all green; `ty check models` / Helm lint /
thumbnail guard / nbstripout report "no files to check").

## Dashboard and catalog test suites

`uv run pytest depictio/tests/models/test_shipped_dashboard_yamls.py -k hic`:
**10/10 passed.** `uv run pytest depictio/tests/models/test_catalog.py`: 97/99
passed; the 2 remaining failures
(`test_committed_json_schema_is_current[catalog.schema.json]`,
`test_committed_json_schema_is_current[output.schema.json]`) are pre-existing
staleness in the top-level `depictio/catalog/*.schema.json` files versus the
current `CatalogEntry`/`CatalogOutput` models, outside this pipeline's scope
(`depictio/catalog/*.schema.json` is not under `cooler/`, `cooltools/`,
`hicexplorer/`, or `multiqc/hicpro.yaml`, and regenerating it touches shared
catalog infrastructure, not a pipeline). A `test_cli_validate_exits_zero_on_
bundled_catalog` failure (cellranger `cluster` dtype mismatch, a different
pipeline's tool built concurrently in this shared worktree) was observed
mid-session and had already been fixed by its owning agent by the time this
report was written.

`depictio-cli run --template nf-core/hic/2.0.0 --data-root
~/Data/depictio-nfcore/hic/2.0.0/megatest --dry-run` completes all 8/8 steps
against the local instance that happened to be running (template resolution,
server/S3 checks, project-config validation and sync, data scanning,
processing, joins, dashboard import). No server-side ingestion (real Delta
table read-back and post-ingest component/card/advanced-viz grounding) was
performed for this report, unlike the cutandrun 3.1 report this one is
modeled on, the brief for this pass scoped ingestion out ("Do NOT ingest (no
server)"). The recipe-level validation above is the substitute evidence of
correctness.

## Dashboard shape

4 tabs (MultiQC, Contact matrix, Compartments, TADs and insulation), 13
sections, 43 components: 9 text, 4 interactive, 5 card, 5 table, 4
advanced_viz, 12 multiqc, 4 kinds bound (`figure`-free, hic has no
plotly-express figure tile; every dedicated-tab visualisation is one of the
three new lot-2 advanced-viz kinds or MultiQC's own panels).

24/26 addressable tiles (card + table + advanced_viz + multiqc) carry a
`use:` (92%): `multiqc/*` 12, `cooltools/*` 8, `cooler/*` 2, `hicexplorer/*`
2. The 2 without `use:` are `hc-sheet-card-libs` and `hc-ref-table-samples`,
both bound to the pipeline-local `samples`/`samplesheet` collections (no
catalog tool, sample hubs are template-specific, not catalog-owned).

## Kinds bound (the pilot)

- `contact_map` -> `cooler/binned_contact_map` (`hc-cm-av-matrix`, "Genome
  architecture" section). Config: `sample_col`, `end1_col`, `end2_col`,
  `colour_scale: Inferno`, `log_scale: true`: the recipe's own column names
  (`chrom1`/`start1`/`chrom2`/`start2`/`count`) already match
  `ContactMapConfig`'s defaults, so no role overrides are needed for the
  required fields.
- `profile` -> `hicexplorer/decay_curve` (`hc-cm-av-decay`, "Distance decay"
  section). Config: `x_title`, `y_title`, `log_x: true`, `log_y: true`.
- `coverage_track` -> `cooltools/compartment_track` (`hc-co-av-track`, "The
  A/B track") and `cooltools/insulation_track` (`hc-td-av-track`, "Insulation
  score").

`ContactMapConfig` landed in `depictio/models/components/advanced_viz/
configs.py` mid-session (it did not exist when first checked, appeared by the
time the config was tuned); all 4 advanced_viz components validate cleanly
against the real Pydantic models as of this report, and
`test_advanced_viz_components_validate` / `test_advanced_viz_survives_the_
component_union` both pass in the full `-k hic` run. No `use:` on a lot-2 kind
is currently blocked.

## MultiQC overlap policy

| Signal | Decision |
|---|---|
| Read counts, per-base/per-sequence quality, GC/N content, duplication, status heatmap | **MultiQC** (`use: multiqc/fastqc`) |
| Two-step mapping rate (global/local), pairing, valid-pair filtering (dangling ends, self-circles, re-ligations), cis/trans split | **MultiQC** (`use: multiqc/hicpro`), HiC-Pro ships a full native module, not duplicated as a dedicated collection |
| Binned, ICE-balanced contact matrix | **Dedicated** (`cooler/contact_matrix`): no MultiQC module reads cooler dumps |
| A/B compartment track (E1/E2/E3) and per-chromosome eigenvalues | **Dedicated** (`cooltools/eigenvector`, `cooltools/eigenvalues`) |
| TAD insulation score and boundary calls | **Dedicated** (`cooltools/insulation`) |
| Distance-decay curve | **Dedicated** (`hicexplorer/distance_decay`): the MultiQC `hicexplorer` plugin, if present, has no module for this pipeline's run |

## Discrepancies

### HC-D1: `megatest.yaml` shipped with 4 dead keys

The manifest guessed `hicpro/*.mapstat`, `*.pairstat`, `*.RSstat`,
`*.mergestat` at the top level; the real files are nested under
`hicpro/stats/<sample>/` with different extensions (`.mmapstat`,
`.mpairstat`, `.mRSstat`, no `.mergestat` equivalent at all). Fixed by
removing the 4 dead keys, the existing `hicpro/stats/*` entry already covers
everything via `fnmatch`'s `.` crossing path separators.

### HC-D2: MultiQC `plots:`/`selected_plot:` is the section name, not the plot title

Confirmed via the installed multiqc 1.35 package source
(`multiqc/modules/{hicpro,fastqc}/*.py`, grepped for `add_section(name=...)`
vs `"title":` config): the value that resolves at runtime
(`extract_multiqc_metadata()`, keyed by bare `section.name`) is a different
string from the plot's internal `title` config (e.g. section name "Read
Mapping" vs. plot title "HiC-Pro: Mapping Statistics"). `template.yaml`'s
`plots:` field is itself decorative/unvalidated (`DCMultiQC` is a permissive
model) but was still authored against the correct value to keep docs and the
dashboard's `selected_plot:` in sync with what actually renders.

### HC-D3: catalog `roles:` should carry only REQUIRED roles; optional roles go in dashboard `config:`

Confirmed via viralrecon's mosdepth `coverage_track` usage and
`mosdepth/genome_coverage.yaml`: `roles:` at the catalog level binds only the
kind's required roles; optional roles (`end`, `sample`, `category`, etc.) are
set per-dashboard via `config: {<role>_col: ...}`. Initially over-specified
`roles:` in `eigenvector.yaml`, `insulation.yaml` and `contact_matrix.yaml`
with optional roles included; narrowed to required-only and moved the
optional bindings to the dashboard's `advanced_viz.config`.

### HC-D4: render-id collision with the derived output short-id

`cooler/contact_matrix.yaml`'s `advanced_viz` render was first given the
explicit `id: contact_matrix`, which collides with the output's own derived
short-id (`"cooler_contact_matrix".removeprefix("cooler_")` = `contact_matrix`)
,  `_load_tool_dir` raises `ValueError` on this. Renamed the render id to
`binned_contact_map`; `use: cooler/binned_contact_map` in the dashboard
reflects the rename (the `card`/`table` renders, which resolve via the
output's own id, kept `use: cooler/contact_matrix`).

### HC-D5: `nf_core_url` requires `github.com/nf-core/modules/...`; pipeline-local modules need `source_url`

`hicexplorer/module.yaml`'s module (`HIC_PLOT_DIST_VS_COUNTS`) is a
pipeline-local module in `nf-core/hic`, not a shared `nf-core/modules` entry.
`_check_identity_urls()` enforces the `github.com/nf-core/modules` authority
on `nf_core_url` specifically; switched the field to `source_url` (no
authority check).

### HC-D6: `int64`/`float64` columns cannot use `Select` as an interactive widget

`INTERACTIVE_COMPATIBILITY["int64"]` is `["Slider", "RangeSlider"]` only ,
`Select` is not permitted on numeric columns (it is for
`category`/`object`). Three filters (`hc-cp-filter-resolution`,
`hc-td-filter-window`, `hc-td-filter-resolution`) were originally authored as
`Select` on `int64` resolution/window columns; switched to `RangeSlider`.

### HC-D7: `ProfileConfig`/`ContactMapConfig` field names differ from a first guess

`ProfileConfig` uses `log_x`/`log_y` (not `x_scale`/`y_scale: log`).
`ContactMapConfig` uses `count_col` (not `value_col`) and British-spelled
`colour_scale` (not `colorscale`), whose `Literal` values are `Viridis`,
`Plasma`, `Inferno`, `Magma`, `Cividis`, `RdBu`, `Spectral` (`"Reds"` is not
valid). Both were caught only by direct inspection of
`depictio/models/components/advanced_viz/configs.py`, since the config
submodels are `extra="forbid"` and give no partial-match diagnostics beyond
"Extra inputs are not permitted".

### HC-D8: schema union at ingest is null-fill, not strict concat

`cooltools/insulation.py` initially assumed a fixed 3-window tuple; the real
run's two resolution files (20 kb, 40 kb) scan *different*, only
partially-overlapping window sizes (300k/500k/1M vs 600k/1M/2M).
`align_lazy_schemas()` (the real ingestion path for a recursive `Table` scan)
unions columns across all matched files and null-fills missing ones per file,
then does a default `pl.concat()`: files with genuinely different column
sets still concatenate successfully via null-fill, not via strict/error
matching. The recipe was rewritten to discover windows dynamically from
`df.columns` and to filter on `n_valid_pixels.is_not_null()` (not
`log2_insulation_score`) to correctly distinguish "this file never scanned
this window" (null) from "this bin is masked at a window that WAS scanned"
(a real `0.0`).

### HC-D9: five concurrent agents transiently broke the shared catalog directory during this session

`load_catalog_entries()` is `@lru_cache(maxsize=1)` and eagerly loads/
validates every tool folder under `depictio/catalog/`: a single malformed
file anywhere in the catalog breaks catalog loading (and therefore
`AdvancedVizLiteComponent` validation) for every pipeline sharing the
worktree, not just the owner. During this session, `bcftools`, `cellbender`,
`cellranger`, `qualimap` and `samtools` tool dirs were each observed broken
at various points (built concurrently by other agents in the same lot-2
worktree) and blocked `test_catalog.py`/`test_shipped_dashboard_yamls.py`
globally; all had been fixed by their owning agents by the time this report
was written, and the full suite now runs clean except for the two
model-schema-staleness failures noted above. Recommend the brief mention this
collision risk explicitly (isolate `_load_tool_dir` validation per own tool
dir as a workaround when the shared suite is red) for the next 6 pipelines.
