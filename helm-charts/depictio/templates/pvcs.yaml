# MongoDB PVC is now handled by StatefulSet volumeClaimTemplates
# No manual PVC needed for StatefulSet

{{- if .Values.minio.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-minio-pvc
  namespace: {{ .Release.Namespace }}
  labels:
    app: minio
spec:
  storageClassName: {{ .Values.storageClass }}
  accessModes:
    - {{ .Values.persistence.minio.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.minio.size }}
{{- end }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-depictio-screenshots-pvc
  namespace: {{ .Release.Namespace }}
  labels:
    app: depictio
spec:
  storageClassName: {{ .Values.storageClass }}
  accessModes:
    - {{ .Values.persistence.screenshots.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.screenshots.size }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-depictio-keys-pvc
  namespace: {{ .Release.Namespace }}
  labels:
    app: depictio
spec:
  storageClassName: {{ .Values.storageClass }}
  accessModes:
    - {{ .Values.persistence.keys.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.keys.size }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-depictio-config-pvc
  namespace: {{ .Release.Namespace }}
  labels:
    app: depictio
spec:
  storageClassName: {{ .Values.storageClass }}
  accessModes:
    - {{ .Values.persistence.config.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.config.size }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-depictio-uploads-pvc
  namespace: {{ .Release.Namespace }}
  labels:
    app: depictio
spec:
  storageClassName: {{ .Values.storageClass }}
  accessModes:
    - {{ .Values.persistence.uploads.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.uploads.size }}
